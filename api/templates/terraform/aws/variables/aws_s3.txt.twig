# - S3 VARIABLES ----------------------------------------------------- #

variable "{{ variables.unique_label }}" {
  type = map(string)
  default = {
    acl = "{{ variables.acl|default("private") }}"
  }
}
