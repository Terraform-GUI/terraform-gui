# - VPC OUTPUTS -------------------------------------------------------- #

output "{{ variables.unique_label }}_id" {
  description = "ID of VPC"
  value       = aws_vpc.{{ variables.unique_label }}.id
}
